# --- Core AWS SDK ---
boto3>=1.34.0             # The standard AWS SDK for Python (talks to Bedrock, S3, DynamoDB)

# --- LangChain & Orchestration ---
langchain>=0.3.0          # The main framework for chaining prompts and logic
langchain-core==1.2.28     # The core framework for chaining prompts and logic
langchain-aws>=0.2.0      # Specialized AWS integrations (optimized for Bedrock)
langchain-community>=0.3.0 # needed for specific tools/utilities not in core
llama-index>=0.10.0       # LlamaIndex - data framework for LLM applications

# --- LLM Provider SDKs ---
openai>=1.30.0            # OpenAI API (GPT-4, GPT-3.5, embeddings, DALL-E)
anthropic>=0.25.0         # Anthropic API (Claude 3, Claude 3.5)

# --- Data Validation & Schema ---
pydantic>=2.8.0           # Essential for validating data structures (inputs/outputs)

# --- Deep Learning Frameworks (commented out - heavy packages) ---
# torch>=2.2.0              # PyTorch - deep learning framework
# torchvision>=0.17.0       # PyTorch vision utilities and pretrained models
# torchaudio>=2.2.0         # PyTorch audio processing
# tensorflow>=2.16.0        # TensorFlow - ML platform
# tensorflow-hub>=0.16.0    # Pre-trained TensorFlow models
# transformers>=4.40.0      # Hugging Face Transformers (works with PyTorch/TF)
# datasets>=2.19.0          # Hugging Face Datasets
# accelerate>=0.30.0        # Hugging Face Accelerate for distributed training
# sentence-transformers>=2.7.0  # Sentence embeddings

# --- Traditional ML & Data Science (commented out - heavy packages) ---
# scikit-learn>=1.4.0       # Traditional ML algorithms
# numpy>=1.26.0             # Numerical computing
# pandas>=2.2.0             # Data manipulation and analysis
# scipy>=1.13.0             # Scientific computing

# --- NLP (commented out - heavy packages) ---
# spacy>=3.7.0              # Industrial-strength NLP
# nltk>=3.8.0               # Natural Language Toolkit

# --- Data Visualization (commented out - heavy packages) ---
# matplotlib>=3.8.0         # Plotting library
# seaborn>=0.13.0           # Statistical data visualization
# plotly>=5.22.0            # Interactive visualizations

# --- Vector Database / Retrieval (commented out - heavy packages) ---
# opensearch-py>=2.6.0      # If you need custom access to OpenSearch Serverless
# chromadb>=0.5.0           # Vector database for embeddings
# faiss-cpu>=1.8.0          # Facebook AI Similarity Search

# --- Utilities ---
requests>=2.32.0          # Standard HTTP library for API calls
python-dotenv>=1.0.0      # For loading local environment variables during dev
tiktoken>=0.7.0           # Token counting (useful for estimating costs before sending to Bedrock)
aiohttp>=3.9.0            # Async HTTP client
tenacity>=8.3.0           # Retry library for API calls
